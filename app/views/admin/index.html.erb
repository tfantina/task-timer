<% provide(:title, "Admin panel") %>
<div class="container">

<h1>Welcome Admin</h1>

Select user to see stats:
<ul class="usr-list">
  <% @users.each do |usr| %>

  <li>
     <%= link_to usr.email_or_user_name, admin_user_tasks_path(usr) %>
    </li>
  <% end %>
  </ul>

<%= button_to "export tasks", tasks_path(format: 'xlsx'), data: {turbolinks: false}, class: "btn btn-primary" %>
</div>